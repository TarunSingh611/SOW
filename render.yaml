services:
  - type: web
    name: sow-project
    env: node
    buildCommand: cd client && npm install && npm run build
    startCommand: cd server && npm install && npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
